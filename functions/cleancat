#--------------------------------------------------------------------
# Grep a file without comments or empty lines
#--------------------------------------------------------------------
perl -lne 'print if ! /^\s*(#.*)?$/' "$*"